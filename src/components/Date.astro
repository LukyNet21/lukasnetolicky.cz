---
interface Props {
  date: Date;
}
const { date } = Astro.props;
const formattedDate = date.toLocaleDateString("en-US", {
  year: "numeric",
  month: "long",
  day: "numeric",
});
---
<span class="date">
  {formattedDate}
</span>